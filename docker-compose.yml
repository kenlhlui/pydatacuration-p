services:
  data-curation-tool:
    build: .
    image: data-curation-tool:latest
    ports:
      - "9005:80"
    env_file:
      - .env
    environment:
      - DOTENV_PATH=/app/.env
    restart: unless-stopped